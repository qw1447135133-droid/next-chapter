(Array.from(document.querySelectorAll('[role="combobox"]')).map((el,i)=>({i,text:(el.innerText||el.textContent||'').replace(/\s+/g,' ').trim(),top:Math.round(el.getBoundingClientRect().top),left:Math.round(el.getBoundingClientRect().left),width:Math.round(el.getBoundingClientRect().width),height:Math.round(el.getBoundingClientRect().height),expanded:el.getAttribute('aria-expanded'),popup:el.getAttribute('aria-haspopup')})).filter(x=>/Seedance 2\.0/i.test(x.text)))
